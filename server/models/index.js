const config = require('../config');
const Sequelize = require('sequelize');

// const sequelize = new Sequelize(
//     config.db.database,
//     config.db.user,
//     config.db.password,
//     config.db.options,
// );

module.exports = {};